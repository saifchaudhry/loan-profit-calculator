volumes:
  postgres_data: {}
  redis_data: {}
  sidekiq_data: {}

services:

  postgres:
    image: postgres:alpine
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: development_loan_profit_calculator
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - 5432:5432

  web:
    build: .
    command: bundle exec rails s -p 3000 -b '0.0.0.0'
    ports:
      - "3000:3000"
    environment:
      RAILS_ENV: ${RAILS_ENV:-development}
      DATABASE_HOST: postgres
      DATABASE_USER: postgres
      DATABASE_PASSWORD: postgres
      DATABASE_NAME: development_loan_profit_calculator
      DATABASE_PORT: 5432
    depends_on:
      - postgres
      - redis

  worker:
    build: .
    depends_on:
      - postgres
      - redis
    env_file:
      - .env
    environment:
      RAILS_ENV: ${RAILS_ENV:-development}
      DATABASE_HOST: postgres
      REDIS_SIDEKIQ_URL: ${REDIS_SIDEKIQ_URL:-redis://redis:6379/0}
      REDIS_CACHE_URL: ${REDIS_CACHE_URL:-redis://redis:6379/1}
      REDIS_URL: ${REDIS_URL:-redis://redis:6379/2}
    volumes:
      - sidekiq_data:/var/lib/sidekiq/data
    command: bundle exec sidekiq -C config/sidekiq.yml

  redis:
    image: "redis:7-alpine"
    ports:
      - 6379
    volumes:
    - ./tmp/redis_data:/var/lib/redis/data